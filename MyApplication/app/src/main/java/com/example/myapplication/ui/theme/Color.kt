package com.example.myapplication.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * =============================================================================
 * Paleta Oficial Aqua-Registro (Material Design 3)
 * =============================================================================
 * Colores corporativos diseñados para maximizar la legibilidad en terreno,
 * bajo luz solar intensa en pontones y centros de cultivo acuícola.
 */

/** Azul claro principal (#0099FF): Barra superior, botones primarios y branding. */
val AquaPrimary = Color(0xFF0099FF)

/** Verde-agua secundario (#00CCCC): Botón flotante FAB, switches activos y acentos marinos. */
val AquaSecondary = Color(0xFF00CCCC)

/** Aqua adicional / comodín (#00E5FF): Color identitario de AquaChile. */
val AquaTertiary = Color(0xFF00E5FF)

/** Fondo suave (#F6F9FD): Superficie general de la aplicación con bajo cansancio visual. */
val AquaBackground = Color(0xFFF6F9FD)

/** Blanco puro (#FFFFFF): Contenedores elevados y tarjetas de formulario. */
val AquaSurface = Color(0xFFFFFFFF)

/** Texto principal (#111827): Slate oscuro para máxima legibilidad táctil bajo el sol. */
val AquaTextPrimary = Color(0xFF111827)

/** Texto secundario (#4B5563): Gris intermedio para leyendas, etiquetas y subtítulos. */
val AquaTextSecondary = Color(0xFF4B5563)

// =============================================================================
// Estados de Seguridad y Dictamen de Buceo
// =============================================================================

/** Indicador de buzo "Apto para Inmersión" y checklist 100% conforme (#16A34A). */
val SafetySuccess = Color(0xFF16A34A)

/** Indicador de precaución o chequeo pendiente (#EAB308). */
val SafetyWarning = Color(0xFFEAB308)

/** Indicador crítico de buzo "No Apto" o falla en implementos obligatorios (#DC2626). */
val SafetyDanger = Color(0xFFDC2626)

/** Fondo verde suave (#DCFCE7) para tarjetas y badges de éxito. */
val SafetyCardBgSuccess = Color(0xFFDCFCE7)

/** Fondo rojo suave (#FEE2E2) para tarjetas de alerta de salud y detención preventiva. */
val SafetyCardBgDanger = Color(0xFFFEE2E2)